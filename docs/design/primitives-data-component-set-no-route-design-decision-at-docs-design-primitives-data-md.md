# Design Decision — primitives-data (pointer)

This file name was derived mechanically from the increment's screens line ("primitives-data
(component set, no route — Design Decision at docs/design/primitives-data.md covering both
the overlay and data sets: every component state, sample-data copy verbatim, tokens, motion,
both themes, the testids above; committed before acceptance per C-13)"). Both entries name
the same screen. The full derived slug is 260 bytes — over the filesystem's 255-byte
filename limit — so this pointer carries the slug truncated at the "…primitives-data-md"
boundary; nothing else about it is decided.

The Design Decision has one home (B-17): **docs/design/primitives-data.md**. It rules the
overlay set (Dialog, Sheet, Popover, DropdownMenu, ContextMenu, Toast) and the data set
(Tabs, Tree, ScrollArea, Resizable panels, DataTable) — every component state, the verbatim
sample-data copy, tokens, motion, both themes and the sixteen contract test ids; nothing is
decided here, and nothing may fork from it in this file.
