// FIXTURE: cubit/no-colour-literals MUST NOT report on this file.
// Every colour arrives as a CSS variable emitted from the token source.

export const surface = {
  background: 'var(--graphite-0)',
  border: 'var(--graphite-200)',
  ring: 'var(--cobalt-500)',
  accent: 'var(--cobalt-600)',
};
