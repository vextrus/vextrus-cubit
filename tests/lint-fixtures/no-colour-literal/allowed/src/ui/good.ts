// R-UI-001: a ui file beside the token source spells no colour — the allowlist is the exact path,
// not the directory.
import { tokens } from "./tokens";

export const brand = tokens["--token-surface-raised"];
