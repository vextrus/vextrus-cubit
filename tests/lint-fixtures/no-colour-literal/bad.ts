// R-UI-001 fixture: colour typed into a component instead of a token.
export const surface = '#0b1220';

export const overlay = 'rgba(11, 18, 32, 0.72)';

export const accent = `oklch(0.72 0.15 172)`;

// pixi.js takes colour as a number, so this is the same escape wearing hex.
export const marker = 0xff3366;

export const markerWithAlpha = 0xff3366aa;
