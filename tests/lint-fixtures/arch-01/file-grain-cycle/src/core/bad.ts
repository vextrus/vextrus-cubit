// ARCH-01: a cycle at file grain. The payload IS the flagged construct (Q-08 declared fixture).
import "./partner"; // RECORDED REASON ARCH01_FILE_GRAIN_CYCLE

export const payload = "file-grain cycle";
