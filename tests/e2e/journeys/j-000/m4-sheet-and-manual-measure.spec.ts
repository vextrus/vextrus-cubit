/**
 * J-000 SEGMENTS: ingest and corroborate a PDF sheet; measure a manual condition; take rooms and finishes; ask the drawings a question
 *
 * M4's leg of the golden path, DECLARED before the milestone lands (AM-09 §3): "an M4 leg (a PDF
 * sheet ingested and corroborated, a manual condition measured, rooms and finishes taken, a question
 * asked of the drawings)".
 *
 * A stub rather than nothing, for the reason m3's is: the roster derives the legs the golden path
 * owes from the Bible's own text — and, since AM-17, from the rider that gave J-000 its M3 and M4
 * segments. Until AM-17 this file carried NO segment line and passed anyway, because the Bible
 * marked no M4 segment for it to drop; the roster now names an M4 segment no m4 leg claims. The
 * increment that ships M4 replaces this `test.fixme` with the walk — it never deletes the file.
 */
import { test } from "@playwright/test";

test.describe("J-000 — Golden Path: M4's leg (AM-09 §3), owed", () => {
  test.fixme("J-000 m4-sheet-and-manual-measure: a PDF sheet ingested and corroborated, a manual condition measured, rooms and finishes taken, and a question asked of the drawings", () => {
    // AM-09 §3. M4 has not landed; the leg is declared, collected and impossible to forget.
  });
});
