/**
 * Deliberate payload for the Q-07 register scan, not product source: a refusal-shaped code the
 * registry does not hold and no vocabulary declares. This is the shape of an orphan, and the scan
 * is proved to say so on it. Nothing imports this file but the scan that reads it.
 */
export const PLANTED_CODE = "FIXTURE_ORPHAN_CODE";
