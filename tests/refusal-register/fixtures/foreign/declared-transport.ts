/**
 * Deliberate payload for the Q-07 register scan, not product source: a name from a foreign
 * vocabulary the transport table declares — refusal-shaped, owned by the transport, and never an
 * answer to a user. The scan is proved to call it foreign here, and never an orphan.
 */
export const TRANSPORT_CODE = "INTERNAL_SERVER_ERROR";
