/**
 * Deliberate payload for the Q-07 register scan, not product source: a registered code spelled as a
 * bare literal in a file that does not import the register — it agrees with the taxonomy by
 * coincidence, and would go on agreeing with a taxonomy that had changed. The missing import is the
 * point of the fixture, so this file imports nothing.
 */
export const SPELLED_CODE = "PRECISION_NOT_APPLIED";
