# Founder seed — inc-000 Foundation owns and extends this (C-06).
node_modules/
.next*/
dist/
coverage/
*.log
.env*
!.env.example

# Toolchain outputs: everything the gate writes while it runs, none of it tracked. Generated SQL
# migrations are not here on purpose — those are reviewed artefacts and belong in the tree.
.storage/
playwright-report/
test-results/
*.tsbuildinfo
.eslintcache
.vitest-cache/
