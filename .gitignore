# Dependencies
node_modules/
cad/.venv/

# Build output
.next/
.next-verify/
out/
dist/
*.tsbuildinfo

# Gate scratch — verify writes here and the tree must stay clean (AC-8)
.cubit-scratch/
coverage/
test-results/
playwright-report/
blob-report/
.playwright/

# docs/traceability.json is deliberately NOT ignored: the gate generates and
# commits it (C-10), and an ignored path cannot be committed by a plain
# `git add`. This increment simply does not create it — tests/toolchain/
# surface.test.ts asserts its absence here.

# Local machine state
.env
.env.local
*.log
.DS_Store
