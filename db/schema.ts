// The path drizzle.config.ts and scripts/lib/lanes.mjs pin (ARCH-02). It is a barrel over the schema
// tree so the pinned path and the tree are always the same schema.
export * from "./schema/index";
