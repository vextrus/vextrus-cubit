-- Retired slot (2026-09-07). This migration created M2's work-item catalogue and `bears` tables
-- (inc-207, merged 2026-08-30 under an engine that has since been rebuilt). M2 was restarted from
-- zero and that work was unmerged; the slot stays so the numbered chain and the tests that name
-- later migrations by number keep their names. It applies nothing.
SELECT 1;
