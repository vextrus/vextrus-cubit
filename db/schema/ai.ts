// R-AI-001's recorded dispositions, read back out of the seam. The builders themselves are lawful
// only in src/core/db.ts (SEAM-TENANT); this file is where drizzle-kit, the drift lane and the live
// suite see the table (ARCH-02).
export { sheetUnderstandingDispositions } from "../../src/core/db";
