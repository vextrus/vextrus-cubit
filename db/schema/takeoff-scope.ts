// R-TO-052's boundary store: the declarations a person makes over one cell of the residue — held
// out of this bill, or out of the project's scope (L-QTY-05's two axes). The definition lives in the
// seam, where the ORM's table builders have their one lawful home (SEAM-TENANT); this file is where
// the generator and the drift lane read it back.
export { scopeDeclarations } from "../../src/core/db";
