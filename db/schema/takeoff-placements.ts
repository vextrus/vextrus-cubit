// L-CAD-07's placement, expansion and levels proposal — the fifth, sixth and seventh stages of the
// stored partition, and the range a person authors for a view whose caption stated none — as the
// schema tree offers them to drizzle-kit. The definitions live in the seam: the ORM's table builders
// are a driver import, and src/core/db.ts is their one lawful home (SEAM-TENANT). This file is where
// the generator and the drift lane read them back.
export { placements, expansionDeferrals, typicalRanges, proposedLevels } from "../../src/core/db";
