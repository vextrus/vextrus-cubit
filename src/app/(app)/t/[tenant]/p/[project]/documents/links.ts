// How long a link minted for this list stays good (I-264).
//
// A link on this screen is followed from the screen: a reader sees the row and opens the document.
// Fifteen minutes is generous for that and short for a URL that has left the page — and a link
// followed after it is answered at the door with its own registered refusal, never here.
export const DOCUMENT_LINK_TTL_SECONDS = 900;
