// The root route. It renders an empty main landmark on purpose: the product's screens arrive with
// their own increments and their own Design Decisions, and copy invented here would be copy no
// string table owns (C-SPINE-PLATFORM).
export default function HomePage() {
  return <main />;
}
