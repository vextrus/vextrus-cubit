// The root route. It renders an empty main landmark on purpose: copy invented here would be copy
// no string table owns (C-SPINE-PLATFORM), and no screen exists yet whose Design Decision rules
// what this route should say.
export default function HomePage() {
  return <main />;
}
