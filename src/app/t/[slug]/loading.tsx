import { LoadingSkeleton } from '../../../ui/primitives/screen-state';

/** R-UI-050 — the tenant home's loading state: a skeleton that keeps the layout. */
export default function TenantLoading() {
  return <LoadingSkeleton />;
}
