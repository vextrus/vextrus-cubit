import { LoadingSkeleton } from '../../../ui/primitives/screen-state';

/** R-UI-050 — the magic-link screen's loading state (see sign-in/loading.tsx). */
export default function MagicLinkLoading() {
  return <LoadingSkeleton />;
}
