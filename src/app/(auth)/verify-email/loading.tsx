import { LoadingSkeleton } from '../../../ui/primitives/screen-state';

/** R-UI-050 — the verify-email screen's loading state (see sign-in/loading.tsx). */
export default function VerifyEmailLoading() {
  return <LoadingSkeleton />;
}
