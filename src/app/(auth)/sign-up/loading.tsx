import { LoadingSkeleton } from '../../../ui/primitives/screen-state';

/** R-UI-050 — the sign-up screen's loading state (see sign-in/loading.tsx). */
export default function SignUpLoading() {
  return <LoadingSkeleton />;
}
