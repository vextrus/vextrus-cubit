import { LoadingSkeleton } from '../../../ui/primitives/screen-state';

/** R-UI-050 — the reset-password screen's loading state (see sign-in/loading.tsx). */
export default function ResetPasswordLoading() {
  return <LoadingSkeleton />;
}
