// R-SPINE-060: S-Design's own table — the two sentences the gallery authors (Decision I-17).
// Everything else the page shows is either a machine identifier read off the derivation — a barrel
// id, an entry key, a state name — or copy the sample's owning Decision already fixed.
export const design = {
  design_gallery_heading: "Design gallery",
  design_gallery_caption: "Every shipped component, rendered in every state it can hold, with sample data.",
} as const;
