// R-SPINE-060: the stored partition's own table. R-UI-024's timeline names every kind SEAM-JOBS
// runs, so the kind that rebuilds a partition owes the word a person reads it as — a step that
// named no work would be a process somebody is watching without being told what it is.
export const partition = {
  job_step_partition: "Sort the drawing into views",
} as const;
