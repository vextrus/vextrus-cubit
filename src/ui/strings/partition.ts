// R-SPINE-060: the stored partition's own table. Today it holds one key — the word the job pattern
// reads a `partition` step as (R-UI-024's timeline names every kind SEAM-JOBS runs), which a kind
// joining the roster owes or it would render nameless. The views themselves have no surface yet.
export const partition = {
  job_step_partition: "Sort the drawing into views",
} as const;
