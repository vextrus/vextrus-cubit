// The frame's jobs tray (R-UI-030), published through the shell barrel beside the bar that carries
// it. Its stylesheet travels with it, so the tray cannot be rendered unstyled.
import "./jobs-tray.css";

export { JobsTray } from "./jobs-tray";
