/**
 * The two menus wear one chrome and offer one item vocabulary, so the variant that grades an item
 * is declared once (B-17) rather than in each menu's module.
 */
export type MenuItemVariant = "default" | "danger";
