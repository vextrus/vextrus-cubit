// The rule-set authoring module (R-SPINE-012, AM-04): the copy the Author edition screen renders and the
// pure diff its grid is drawn from — the law of the screen, with no tier above core in it (ARCH-01:
// a module imports core and its own module, so the screen itself lives beside its route.)
export { authoredValues, diffParameters, sameFigure } from "./diff";
export type { ParameterDiffRow } from "./diff";
export { rulesetAuthorStrings } from "./strings";
export type { RulesetAuthorStringKey } from "./strings";
