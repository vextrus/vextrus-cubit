// R-TO-032's foundations, at their door: the five class readers this area writes, the rules they
// offer under, and the closed roster of codes they answer with.
//
// The rails themselves stand in the files their clause stands in — the prisms with L-FRM-02, the pit
// and the blinding with L-FRM-04, the piles with AM-06 §2 — and this file is where the area publishes
// them. The area's ROSTER, which keys a rail by the kind it measures, is `../foundations.ts`: a rail
// is selected per kind (L-MEA-08), and `rcc.concrete` is measured by one function composed of this
// area's concrete reader and the columns' (riskNotes (1)).

export { blindingRail, excavationRail, BLINDING_RULE_ID, EXCAVATION_RULE_ID } from "./earthwork";
export { foundationConcreteRail, FOUNDATION_PRISM_POLY_RULE_ID, FOUNDATION_PRISM_RECT_RULE_ID, PILE_CONCRETE_RULE_ID } from "./concrete";
export { pileCountRail, pileLengthRail, PILE_COUNT_RULE_ID, PILE_LENGTH_RULE_ID } from "./piling";
export { FOUNDATIONS_RAIL_CODES } from "./read";
export type { FoundationsRailCode } from "./read";
