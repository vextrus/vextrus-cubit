import { router } from '../trpc';

/**
 * The model seam's surface (L-AI-01). Mounted from day one so the increment that
 * lands `callModel` adds procedures and never touches composition (AS-A1).
 */
export const aiRouter = router({});
